import java.io.File

import org.scalatest.{FlatSpec, Matchers}


class PizzaOptimizerSpec extends FlatSpec with Matchers {
/*
  "pizza optimizer" should "pass input #01" in {
    val input = List(PizzaRequest(0, 3),PizzaRequest(1, 9),PizzaRequest(2, 5)).iterator
    new PizzaWaitingOptimizer(3, input)
      .minAvg should equal(8)
  }
  "pizza optimizer" should "pass input #00" in {
    val input = List(PizzaRequest(0, 3),PizzaRequest(1, 9),PizzaRequest(2, 6)).iterator
    new PizzaWaitingOptimizer(3, input)
      .minAvg should equal(9)
  }
  */
}
